import React, { Component } from "react";
import { Search } from "../Search";

export class Header extends Component {
  render() {
    return (
      <div>
        <Search />
      </div>
    );
  }
}
